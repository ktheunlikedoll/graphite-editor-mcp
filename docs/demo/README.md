# Rendered demos

Both artifacts rendered headlessly by this MCP server through the pinned CLI.

- `evidence-engine-banner.png` — hand-authored inline document (19 nodes): noise →
  Levels → GradientMap field, positioned vector rules/squares, TextNode → vector
  text, transform-wrapped layers stacked via ExtendNode. Rendered with quarantine
  metadata; deterministic (byte-identical re-renders).
- `kinetic-01-interference.gif` — 24-frame seamless loop @ 12fps: `RepeatRadialNode`
  star (AnimationTime → Math → rotation) over a `RepeatNode` line field with
  ReadIndex-driven per-copy spacing. Op-art interference in a three-color palette.
